---
phase: 02-agency-page-reference-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/services.ts
  - src/data/plans.ts
  - src/data/portfolio.ts
autonomous: true
requirements: [PORT-01]

must_haves:
  truths:
    - "Visitor reads final, non-placeholder copy for all 6 Agency services on the Agency page"
    - "Visitor reads final, non-placeholder feature copy for all 3 plans (Starter/Growth/Premium) on the Agency page"
    - "Visitor sees exactly 3 specific, finished-reading Agency case studies (Lumen Skincare, Northfield Outdoor, Almora Home) in the portfolio section"
  artifacts:
    - path: "src/data/services.ts"
      provides: "6 Service entries with final brand-voice descriptions, zero 'Placeholder' strings"
      contains: "Brand Strategy"
    - path: "src/data/plans.ts"
      provides: "3 Plan entries with final feature bullets, zero '(placeholder ...)' annotations"
      contains: "Content calendar & posting cadence"
    - path: "src/data/portfolio.ts"
      provides: "Exactly 3 PortfolioItem entries with final title/description, zero 'Placeholder Client' strings"
      contains: "Lumen Skincare"
  key_links:
    - from: "src/data/services.ts"
      to: "src/components/agency/ServicesGrid.tsx (via Agency.tsx import, unchanged this plan)"
      via: "services array prop, no shape change"
      pattern: "export const services: Service\\[\\]"
    - from: "src/data/portfolio.ts"
      to: "src/components/agency/PortfolioShowcase.tsx (via Agency.tsx import, unchanged this plan)"
      via: "portfolio array prop, no shape change"
      pattern: "export const portfolio: PortfolioItem\\[\\]"
---

<objective>
Rewrite the three Agency data files (`services.ts`, `plans.ts`, `portfolio.ts`) with the final, approved brand-voice copy from `02-UI-SPEC.md`'s Copywriting Contract, removing every "Placeholder" string and `(placeholder ...)` annotation — including the ones buried mid-string inside plan feature bullets — per locked decisions D-02, D-03, D-07.

Purpose: Phase 2 is the "reference implementation" — CONTEXT.md D-02 requires the Agency page to read as genuinely finished after this phase, not half-placeholder pending Phase 4. `Agency.tsx` already renders these three arrays via `ServicesGrid`, `PlanCard`, and `PortfolioShowcase` with zero code changes needed — this plan only replaces the content payload.
Output: `src/data/services.ts`, `src/data/plans.ts`, `src/data/portfolio.ts` updated in place with final copy; no other files touched; no type/shape changes.
</objective>

<execution_context>
@/Users/nabildorhmi/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/workflows/execute-plan.md
@/Users/nabildorhmi/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agency-page-reference-implementation/02-CONTEXT.md
@.planning/phases/02-agency-page-reference-implementation/02-UI-SPEC.md

<interfaces>
<!-- Existing types this plan's data must continue to satisfy — src/types/index.ts, unchanged this plan -->

export interface Service {
  title: string;
  description: string;
}

export interface PortfolioItem {
  title: string;
  category: string;
  description: string;
}

export interface Plan {
  id: string;
  name: string;
  tagline: string;
  features: string[];
  highlighted?: boolean;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite services.ts and plans.ts with final copy (D-02, D-07)</name>
  <files>src/data/services.ts, src/data/plans.ts</files>
  <read_first>
    src/data/services.ts (current: file-level comment "// Placeholder content — replace with real ROUH Agency service copy." on line 3; 6 entries each with a "Placeholder copy: ..." prefixed description)
    src/data/plans.ts (current: file-level comment lines 3-4; Starter/Growth/Premium entries with `features` arrays containing "Content calendar (placeholder cadence)" and "Paid media management (placeholder budget tier)" mid-string annotations)
    .planning/phases/02-agency-page-reference-implementation/02-UI-SPEC.md (Copywriting Contract → "Plans section" table and "Services section" table — exact final copy to use verbatim)
  </read_first>
  <action>
    In services.ts: delete the file-level comment on line 3. Keep all 6 `title` values unchanged (Brand Strategy, Content Production, Social Media Management, Paid Media & Growth, Web & Digital Design, Influencer & Partnerships). Replace each `description` string with the UI-SPEC final copy verbatim: Brand Strategy → "Positioning, identity, and messaging that give a brand a clear point of view — and a reason to be chosen over the next name on the list."; Content Production → "Photo, video, and social content shot and edited to one visual standard, so every platform looks like it belongs to the same brand."; Social Media Management → "Day-to-day ownership of your channels — calendars, captions, community replies — so posting never falls to whoever has five minutes free."; Paid Media & Growth → "Campaign strategy and media buying across paid social and search, built around the metrics that actually move revenue."; Web & Digital Design → "Websites and digital experiences designed to convert on the first visit and built to hold up as the brand grows."; Influencer & Partnerships → "Creator sourcing and partnership management matched to brand fit, not just follower count."
    In plans.ts: replace the two-line file-level comment (lines 3-4) with a single-line comment keeping only the no-pricing rationale: "// Agency plans intentionally show no pricing — the flow is \"pick a plan, tell us about you, book a call.\"" Keep all `id`, `name`, `tagline`, and `highlighted` values unchanged. Replace each plan's `features` array exactly as follows — Starter: ["Brand & social audit", "Content calendar & posting cadence", "1 core platform managed", "Monthly performance report"]; Growth: ["Everything in Starter", "Multi-platform content production", "Paid social & search media management", "Quarterly strategy review"]; Premium: ["Everything in Growth", "Dedicated account & creative team", "Influencer & partnership sourcing", "Priority turnaround & reporting"]. Do not introduce any `$` figures, spend thresholds, or channel counts per UI-SPEC's no-price-implication rule.
  </action>
  <verify>
    <automated>grep -ci placeholder src/data/services.ts src/data/plans.ts | grep -v ':0' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci placeholder src/data/services.ts` returns 0
    - `grep -ci placeholder src/data/plans.ts` returns 0
    - `grep -c "Content calendar & posting cadence" src/data/plans.ts` returns 1
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (no type errors introduced)
  </acceptance_criteria>
  <done>services.ts has 6 entries with final descriptions and no placeholder string; plans.ts has 3 entries with final feature bullets and no placeholder annotation; both files type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite portfolio.ts with 3 final case-study entries (D-03)</name>
  <files>src/data/portfolio.ts</files>
  <read_first>
    src/data/portfolio.ts (current: file-level comment line 3 "// Placeholder case studies — swap in real client work when available."; 3 entries titled "Placeholder Client A/B/C" with identical generic description "Placeholder case study copy describing scope, approach, and results.")
    .planning/phases/02-agency-page-reference-implementation/02-UI-SPEC.md (Copywriting Contract → "Portfolio section" table — exact final copy, and the note that Lumen Skincare/Northfield Outdoor intentionally reuse client names already established in src/data/testimonials.ts)
  </read_first>
  <action>
    Delete the file-level comment on line 3. Replace the 3 entries exactly as follows, keeping each entry's existing `category` value unchanged (Brand Launch, Social Growth, Web & Digital): entry 1 — title "Lumen Skincare", description "Repositioned a DTC skincare brand ahead of a national retail launch — new identity, messaging system, and a content push that moved funnel numbers within the first month."; entry 2 — title "Northfield Outdoor", description "Rebuilt a founder-run outdoor brand's social presence from scratch — consistent posting cadence, a creative system, and a community that grew alongside the product line, on the timeline we committed to."; entry 3 — title "Almora Home", description "Designed and shipped a new e-commerce site for a home goods brand — from information architecture to checkout — cutting page load time in half and lifting mobile conversion in the first release." Do not add a 4th entry or remove any entry — exactly 3 items, matching `PortfolioShowcase.tsx`'s locked `sm:grid-cols-3` layout (D-03).
  </action>
  <verify>
    <automated>grep -ci placeholder src/data/portfolio.ts; grep -c "title:" src/data/portfolio.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci placeholder src/data/portfolio.ts` returns 0
    - `grep -c "title:" src/data/portfolio.ts` returns 3 (exactly 3 entries, no expansion)
    - `grep -c "Lumen Skincare" src/data/portfolio.ts` returns 1 and `grep -c "Northfield Outdoor" src/data/portfolio.ts` returns 1
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>portfolio.ts contains exactly 3 case-study entries with final, specific, result-oriented copy and no placeholder string remaining.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | This plan adds no new trust boundary — it edits three static, developer-authored TypeScript data modules bundled at build time. No user input, no forms, no network calls, no new rendering path (existing `ServicesGrid`/`PlanCard`/`PortfolioShowcase` components already render these arrays unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-02-01 | Tampering (XSS via unescaped content) | services.ts / plans.ts / portfolio.ts strings rendered as JSX text | accept | Not applicable in practice — all content is static, developer-authored copy interpolated via JSX text nodes (React auto-escapes by default); no `dangerouslySetInnerHTML`, no user-supplied input reaches these files. Low severity, no mitigation needed beyond existing React defaults. |
| T-02-02 | Information Disclosure | Portfolio case-study copy (fictional client names/results) | accept | Content is intentionally fictional/illustrative per CONTEXT.md D-03 (no real client data, no confidential information disclosed). |
</threat_model>

<verification>
Run `npx tsc --noEmit -p tsconfig.app.json` from the repo root — must exit 0. Run `grep -ci placeholder src/data/services.ts src/data/plans.ts src/data/portfolio.ts` — every file must report 0 matches. Manually load `/agency` in a dev server (`npm run dev`) and confirm Services, Plans, and Portfolio ("Selected work") sections render the new copy with no visible "Placeholder" string anywhere.
</verification>

<success_criteria>
- Zero occurrences of the case-insensitive string "placeholder" remain in services.ts, plans.ts, or portfolio.ts.
- `Service`, `Plan`, `PortfolioItem` type shapes are unchanged (no new/removed fields).
- TypeScript compiles with zero new errors.
- Portfolio array has exactly 3 entries; Plans array has exactly 3 entries (Starter/Growth/Premium unchanged); Services array has exactly 6 entries.
</success_criteria>

<output>
Create `.planning/phases/02-agency-page-reference-implementation/02-01-SUMMARY.md` when done
</output>
