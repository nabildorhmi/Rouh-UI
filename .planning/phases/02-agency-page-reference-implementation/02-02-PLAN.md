---
phase: 02-agency-page-reference-implementation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/agency/PortfolioShowcase.tsx
  - src/pages/Agency.tsx
autonomous: true
requirements: [PORT-01, TRUST-01, TRUST-04]

must_haves:
  truths:
    - "Visitor sees the portfolio gradient box with no visible 'Placeholder image' label text"
    - "Visitor encounters the Plans section immediately after the hero, before Services and Portfolio (D-01)"
    - "Visitor can read testimonials from Agency clients on the Agency page, rendered via the shared TestimonialsSection"
    - "Visitor can view a 'how it works' process section explaining the steps of engaging the Agency, rendered via the shared ProcessSection"
    - "Visitor can still select a plan and open the ReservationModal booking flow after the reorder (existing functionality preserved)"
  artifacts:
    - path: "src/components/agency/PortfolioShowcase.tsx"
      provides: "Gradient placeholder box with the 'Placeholder image' span removed, layout/grid/gradient classes untouched"
      contains: "aspect-[4/3] rounded-2xl bg-gradient-to-br"
    - path: "src/pages/Agency.tsx"
      provides: "Reordered page (Hero → Plans → Services → Portfolio → Testimonials → Process) with TestimonialsSection/ProcessSection composed and ReservationModal preserved as last element"
      contains: "TestimonialsSection"
      min_lines: 60
  key_links:
    - from: "src/pages/Agency.tsx"
      to: "src/data/testimonials.ts"
      via: "testimonials.filter(t => t.category === \"agency\" || t.category === \"both\")"
      pattern: "category === \"agency\""
    - from: "src/pages/Agency.tsx"
      to: "src/data/process.ts"
      via: "processSteps.agency passed as steps prop"
      pattern: "processSteps\\.agency"
    - from: "src/pages/Agency.tsx"
      to: "src/components/shared/ReservationModal.tsx"
      via: "selectedPlan state drives open={!!selectedPlan} prop, unchanged wiring"
      pattern: "open=\\{!!selectedPlan\\}"
---

<objective>
Remove the literal "Placeholder image" label from `PortfolioShowcase.tsx` (D-04) and reorder `Agency.tsx`'s sections to Hero → Plans → Services → Portfolio → Testimonials → Process (D-01), composing the existing shared `TestimonialsSection` (filtered to agency-category clients) and `ProcessSection` (agency process steps) for the first time on this page, while preserving the `selectedPlan` state and `ReservationModal` wiring exactly.

Purpose: Delivers TRUST-01 and TRUST-04 (testimonials and process sections did not previously exist on the Agency page) and completes PORT-01's "polished, complete" bar by removing the placeholder-announcing label. D-01's reorder surfaces the primary booking mechanism (Plans → PlanCard → ReservationModal) immediately after the hero to reduce friction to "book a call," per explicit user instruction in CONTEXT.md.
Output: `PortfolioShowcase.tsx` with an empty gradient box (no placeholder text); `Agency.tsx` restructured with the locked section order, two new section compositions, and unchanged plan-selection/modal behavior.
</objective>

<execution_context>
@/Users/nabildorhmi/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/workflows/execute-plan.md
@/Users/nabildorhmi/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agency-page-reference-implementation/02-CONTEXT.md
@.planning/phases/02-agency-page-reference-implementation/02-UI-SPEC.md
@src/pages/DevDesignPreview.tsx

<interfaces>
<!-- Existing shared component contracts this plan composes against — zero code changes to these files -->

From src/components/shared/TestimonialsSection.tsx:
interface TestimonialsSectionProps {
  testimonials: Testimonial[];
  eyebrow?: string;
  title: string;
  description?: string;
}
// Renders its own <section className="mx-auto max-w-6xl px-4 sm:px-6 py-20 sm:py-28"> wrapper internally.
// Returns null if testimonials.length === 0 — do not wrap in an additional <section>.

From src/components/shared/ProcessSection.tsx:
interface ProcessSectionProps {
  steps: ProcessStep[];
  eyebrow?: string;
  title: string;
  description?: string;
}
// Renders its own <section className="mx-auto max-w-6xl px-4 sm:px-6 py-20 sm:py-28"> wrapper internally.
// Returns null if steps.length === 0 — do not wrap in an additional <section>.

From src/data/process.ts:
export const processSteps: { agency: ProcessStep[]; podcast: ProcessStep[] };
// Pre-grouped object, not a flat array — select processSteps.agency directly, no .filter() needed.

From src/data/testimonials.ts (category values already present: "agency" x2, "podcast" x2, none "both" yet):
export interface Testimonial { id: string; category: "agency" | "podcast" | "both"; quote: string; name: string; role: string; }

Proven composition pattern (src/pages/DevDesignPreview.tsx lines 12-18, verbatim filter to replicate):
const agencyTestimonials = testimonials.filter(
  (t) => t.category === "agency" || t.category === "both"
);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove "Placeholder image" label from PortfolioShowcase.tsx (D-04)</name>
  <files>src/components/agency/PortfolioShowcase.tsx</files>
  <read_first>
    src/components/agency/PortfolioShowcase.tsx (current, 25 lines total — the `<span className="text-xs font-bold uppercase tracking-widest text-orange/70">Placeholder image</span>` sits on lines 10-12 inside the gradient `<div>`, preceded by the comment `{/* Placeholder visual — swap for a real project image */}` on line 8; the separate `category`/`title`/`description` block is on lines 14-20, outside this div)
    .planning/phases/02-agency-page-reference-implementation/02-UI-SPEC.md ("D-04 label decision" paragraph — box renders empty, no replacement text)
  </read_first>
  <action>
    Remove exactly two things from the file: the comment `{/* Placeholder visual — swap for a real project image */}` and the `<span className="text-xs font-bold uppercase tracking-widest text-orange/70">Placeholder image</span>` element nested inside it. Leave the outer `<div className="aspect-[4/3] rounded-2xl bg-gradient-to-br from-gold/20 to-orange/20 flex items-center justify-center">` shell in place with all four classes intact, now with empty content (self-closing or empty body). Do not touch the `category`/`title`/`description` block below the box, the `grid gap-6 sm:grid-cols-3` wrapper, or the component's function signature/props (`items: PortfolioItem[]`).
  </action>
  <verify>
    <automated>grep -ci "placeholder" src/components/agency/PortfolioShowcase.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "placeholder" src/components/agency/PortfolioShowcase.tsx` returns 0
    - `grep -c "aspect-\[4/3\] rounded-2xl bg-gradient-to-br from-gold/20 to-orange/20" src/components/agency/PortfolioShowcase.tsx` returns 1 (box classes untouched)
    - `grep -c "item.category" src/components/agency/PortfolioShowcase.tsx` returns 1 (category tag block untouched)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>The gradient box renders with no visible label text; its classes, the category/title/description block, and the grid layout are byte-for-byte unchanged otherwise.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reorder Agency.tsx to Hero→Plans→Services→Portfolio→Testimonials→Process and wire new sections (D-01)</name>
  <files>src/pages/Agency.tsx</files>
  <read_first>
    src/pages/Agency.tsx (current 62-line file — full content already in context via this plan's execution; current order is Hero (lines 18-24) → Services (26-29) → Portfolio (31-37) → Plans (39-51) → ReservationModal (53-59); `selectedPlan` state on line 14, `PlanCard onSelect={setSelectedPlan}` on line 48)
    src/pages/DevDesignPreview.tsx (proven filter + composition pattern for TestimonialsSection/ProcessSection — copy the filter and prop-passing shape, not the file structure)
    .planning/phases/02-agency-page-reference-implementation/02-UI-SPEC.md (section "Composition-specific: page section order and inter-section spacing" table — exact target padding per section; section "Testimonials section composition" and "Process section composition" — exact eyebrow/title/description copy to pass)
  </read_first>
  <action>
    Restructure the JSX returned by `Agency()` into this exact order: (1) Hero — unchanged, `pt-16 sm:pt-24 pb-16`. (2) Plans — move this whole block (the `<section>` containing the `SectionHeading eyebrow="Plans"` and the `plans.map` → `PlanCard` grid) to position 2, immediately after Hero; change its wrapping `<section>` className from `mx-auto max-w-6xl px-4 sm:px-6 py-20` to `mx-auto max-w-6xl px-4 sm:px-6 py-16 sm:py-20`. (3) Services — unchanged content and className (`mx-auto max-w-6xl px-4 sm:px-6 pb-20`), now at position 3. (4) Portfolio ("Selected work") — unchanged content and className (`bg-black/[0.02] border-y border-black/5` outer, `mx-auto max-w-6xl px-4 sm:px-6 py-20` inner), now at position 4; also remove the now-stale comment `{/* Placeholder portfolio — swap in real case studies/logos */}` immediately above the `<PortfolioShowcase items={portfolio} />` call since the data is no longer placeholder. (5) Add a new `<TestimonialsSection>` element (no wrapping `<section>` — the component owns its own wrapper) immediately after Portfolio, with props `testimonials={agencyTestimonials}`, `eyebrow="Client feedback"`, `title="What clients say about working with us"`, `description="A few words from brands we've helped move faster."`. (6) Add a new `<ProcessSection>` element (no wrapping `<section>`) immediately after Testimonials, with props `steps={processSteps.agency}`, `eyebrow="How it works"`, `title="From first call to shipped work"`, `description="No lengthy onboarding — just a clear path from conversation to results."`. (7) `<ReservationModal>` — unchanged props (`open={!!selectedPlan}`, `onClose={() => setSelectedPlan(null)}`, `mode="agency"`, `selectedTierName={selectedPlan?.name ?? ""}`, `calendlyUrl={CALENDLY_LINKS.agency}`), remains the final element in the returned fragment regardless of the Plans section's new position. Add four new imports at the top: `import { testimonials } from "../data/testimonials";`, `import { processSteps } from "../data/process";`, `import { TestimonialsSection } from "../components/shared/TestimonialsSection";`, `import { ProcessSection } from "../components/shared/ProcessSection";`. Add one new constant before the `return` statement, inside the component body, directly below the `useState` line: `const agencyTestimonials = testimonials.filter((t) => t.category === "agency" || t.category === "both");`. Do not modify the `useState<Plan | null>(null)` declaration, the `PlanCard onSelect={setSelectedPlan}` prop, or any `ReservationModal` prop value.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json && grep -n "SectionHeading\|TestimonialsSection\|ProcessSection\|ServicesGrid\|PortfolioShowcase\|ReservationModal" src/pages/Agency.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
    - Running `grep -n "eyebrow=\"Plans\"\|ServicesGrid\|PortfolioShowcase\|TestimonialsSection\|ProcessSection\|ReservationModal" src/pages/Agency.tsx` returns line numbers in that exact ascending order (Plans SectionHeading before ServicesGrid, before PortfolioShowcase, before TestimonialsSection, before ProcessSection, before ReservationModal)
    - `grep -c "useState<Plan | null>(null)" src/pages/Agency.tsx` returns 1 (state declaration preserved)
    - `grep -c "open={!!selectedPlan}" src/pages/Agency.tsx` returns 1 (modal wiring preserved)
    - `grep -c "py-16 sm:py-20" src/pages/Agency.tsx` returns 1 (Plans section repadded per UI-SPEC)
    - `grep -ci "placeholder" src/pages/Agency.tsx` returns 0 (stale placeholder-portfolio comment removed)
  </acceptance_criteria>
  <done>Agency.tsx renders Hero, then Plans, then Services, then Portfolio, then TestimonialsSection (agency-filtered data), then ProcessSection (processSteps.agency), then ReservationModal last; plan-selection state and modal props are unchanged from before the reorder; TypeScript compiles with zero new errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A (existing boundary reused unchanged) | `PlanCard` selection → `ReservationModal`/`ReservationForm` is the only interactive/input boundary on this page; this plan does not touch `ReservationForm.tsx` or add any new input field. It only changes JSX section order and the gradient box's inner text (both static, non-input surfaces). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-02-03 | Tampering | `Agency.tsx` JSX reorder accidentally detaching `ReservationModal` from `selectedPlan` state | mitigate | Task 2's acceptance criteria explicitly grep-verify `useState<Plan \| null>(null)` and `open={!!selectedPlan}` remain present post-edit, catching the exact regression RESEARCH.md Pitfall 2 warns about (accidental loss of state/modal wiring during a cut-paste reorder) |
| T-02-04 | Tampering (unintended UI regression) | `PortfolioShowcase.tsx` gradient box losing its `aspect-[4/3]`/`rounded-2xl`/gradient classes during label removal | mitigate | Task 1's acceptance criteria grep-verify the exact box className string is unchanged after the edit (RESEARCH.md Pitfall 3) |
| T-02-05 | Information Disclosure | New `TestimonialsSection` composition rendering `testimonials.ts` entries | accept | Data is existing, already-approved fictional client testimonials from Phase 1 (D-05); no new or real client data introduced |
</threat_model>

<verification>
Run `npx tsc --noEmit -p tsconfig.app.json` — must exit 0. Run `npm run dev` and load `/agency` in a browser: confirm section order top-to-bottom is Hero, Plans, Services, "Selected work" (Portfolio), a testimonials section ("Client feedback" eyebrow), a process section ("How it works" eyebrow), then confirm clicking "Select this plan" on any `PlanCard` still opens the `ReservationModal` with the correct plan name. Confirm the portfolio gradient boxes show no "Placeholder image" text.
</verification>

<success_criteria>
- Agency.tsx section order matches D-01 exactly: Hero → Plans → Services → Portfolio → Testimonials → Process → ReservationModal (last, unconditional).
- `TestimonialsSection` renders exactly the 2 existing agency-category testimonials (Sarah Chen, Marcus Webb) with no page-specific code inside the shared component.
- `ProcessSection` renders the 4 existing `processSteps.agency` steps with no page-specific code inside the shared component.
- Selecting any plan still opens `ReservationModal` with `mode="agency"` and the correct `selectedTierName`.
- No visible "Placeholder image" text remains in the portfolio gradient boxes.
- Zero new TypeScript errors; zero new npm packages added.
</success_criteria>

<output>
Create `.planning/phases/02-agency-page-reference-implementation/02-02-SUMMARY.md` when done
</output>
