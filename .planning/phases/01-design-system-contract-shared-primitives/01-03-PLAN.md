---
phase: 01-design-system-contract-shared-primitives
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/pages/DevDesignPreview.tsx
  - src/App.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "At least one micro-interaction (scroll-reveal) is visibly working somewhere in the app, confirming motion/tw-animate-css tooling is correctly wired (Roadmap Success Criterion 4)"
    - "TestimonialsSection is reachable in a running dev server and renders correctly with both an agency-tagged and a podcast-tagged data slice"
    - "ProcessSection is reachable in a running dev server and renders correctly with both the agency and podcast step sequences"
    - "prefers-reduced-motion fully disables the scroll-reveal animation rather than toning it down (D-06)"
  artifacts:
    - path: "src/pages/DevDesignPreview.tsx"
      provides: "Walking-skeleton verification route rendering both shared components against both data slices"
      contains: "TestimonialsSection"
    - path: "src/App.tsx"
      provides: "Route registration making the verification page reachable in the browser"
      contains: "dev/design-preview"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/DevDesignPreview.tsx"
      via: "<Route path=\"/dev/design-preview\" element={<DevDesignPreview />} />"
      pattern: "DevDesignPreview"
    - from: "src/pages/DevDesignPreview.tsx"
      to: "src/data/testimonials.ts"
      via: "import + filter by category"
      pattern: "testimonials.filter"
    - from: "src/pages/DevDesignPreview.tsx"
      to: "src/data/process.ts"
      via: "import processSteps.agency / processSteps.podcast"
      pattern: "processSteps\\."
---

<objective>
Wire this phase's walking skeleton together: a temporary, additive-only dev route that renders `TestimonialsSection` and `ProcessSection` against both an agency-tagged and a podcast-tagged sample-data slice, making the scroll-reveal micro-interaction visibly working somewhere in the running app — the concrete proof-of-wiring Roadmap Success Criterion 4 requires — then get human confirmation it looks and behaves correctly before the phase is considered done.

Purpose: per 01-RESEARCH.md's Architecture Patterns and Pitfall 3, the proof that this phase's tooling and components work must be a real, reachable render using the actual shared components and actual sample data — not a throwaway spike disconnected from `TestimonialsSection`/`ProcessSection`, and not something that has to be rebuilt when Phase 2 wires the real Agency page. This route is explicitly temporary infrastructure scaffolding (not production page integration — that's Phase 2/3's job) and should be removed once a real page renders these sections with real page copy.
Output: `src/pages/DevDesignPreview.tsx` (new), one additive `<Route>` line in `src/App.tsx`, and a confirmed-working scroll-reveal interaction.
</objective>

<execution_context>
@/Users/nabildorhmi/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/workflows/execute-plan.md
@/Users/nabildorhmi/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-system-contract-shared-primitives/01-UI-SPEC.md
@.planning/phases/01-design-system-contract-shared-primitives/01-RESEARCH.md
@.planning/phases/01-design-system-contract-shared-primitives/01-02-SUMMARY.md

<interfaces>
<!-- Current App.tsx — exact insertion point for the new route -->
From src/App.tsx (current, full file):
```tsx
import { BrowserRouter, Routes, Route } from "react-router-dom";
import { Layout } from "./components/layout/Layout";
import { Home } from "./pages/Home";
import { Agency } from "./pages/Agency";
import { Podcast } from "./pages/Podcast";
import { Contact } from "./pages/Contact";

function App() {
  return (
    <BrowserRouter>
      <Layout>
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/agency" element={<Agency />} />
          <Route path="/podcast" element={<Podcast />} />
          <Route path="/contact" element={<Contact />} />
        </Routes>
      </Layout>
    </BrowserRouter>
  );
}

export default App;
```

<!-- Testimonial/ProcessStep sample data shape from Plan 01-01, and shared component props from Plan 01-02 -->
```typescript
export const testimonials: Testimonial[]; // id, category: "agency"|"podcast"|"both", quote, name, role
export const processSteps: { agency: ProcessStep[]; podcast: ProcessStep[] };

interface TestimonialsSectionProps { testimonials: Testimonial[]; eyebrow?: string; title: string; description?: string }
interface ProcessSectionProps { steps: ProcessStep[]; eyebrow?: string; title: string; description?: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the verification route and wire it into the router</name>
  <files>src/pages/DevDesignPreview.tsx, src/App.tsx</files>
  <read_first>src/App.tsx, src/data/testimonials.ts, src/data/process.ts, src/components/shared/TestimonialsSection.tsx, src/components/shared/ProcessSection.tsx</read_first>
  <action>
Create src/pages/DevDesignPreview.tsx as named export `DevDesignPreview`. Top-of-file comment: mark this explicitly as TEMPORARY Phase 1 walking-skeleton verification scaffolding, to be removed once Phase 2/3 wire `TestimonialsSection`/`ProcessSection` into the real Agency/Podcast pages with real page copy — not production content. Import `testimonials` from `../data/testimonials`, `processSteps` from `../data/process`, `TestimonialsSection` and `ProcessSection` from `../components/shared/TestimonialsSection` / `../components/shared/ProcessSection`.

Inside the component: derive `agencyTestimonials = testimonials.filter((t) => t.category === "agency" || t.category === "both")` and `podcastTestimonials = testimonials.filter((t) => t.category === "podcast" || t.category === "both")`. Render, in order: `<TestimonialsSection testimonials={agencyTestimonials} eyebrow="Client feedback" title="What clients say" />`, `<ProcessSection steps={processSteps.agency} eyebrow="How it works" title="Working with the agency" />`, `<TestimonialsSection testimonials={podcastTestimonials} eyebrow="Renter feedback" title="What renters say" />`, `<ProcessSection steps={processSteps.podcast} eyebrow="How it works" title="Booking the studio" />`. Wrap all four in a `<main>` element — no `Header`/`Footer`/`Layout` wrapping needed beyond what `App.tsx`'s existing `<Layout>` already provides.

Edit src/App.tsx: add `import { DevDesignPreview } from "./pages/DevDesignPreview";` alongside the other page imports, and add exactly one new line `<Route path="/dev/design-preview" element={<DevDesignPreview />} />` inside the existing `<Routes>` block (after the `/contact` route). Do not modify any existing route, import, or the `Layout`/`BrowserRouter` structure — this is a strictly additive, one-line route registration.
  </action>
  <acceptance_criteria>
    - `grep -c 'dev/design-preview' src/App.tsx` equals 1
    - `grep -c 'import { DevDesignPreview }' src/App.tsx` equals 1
    - src/pages/DevDesignPreview.tsx imports and renders exactly 2 `TestimonialsSection` instances and exactly 2 `ProcessSection` instances
    - `grep -c 'TEMPORARY' src/pages/DevDesignPreview.tsx` is at least 1 (marks the file as Phase 1 scaffolding, not production content)
    - `npm run build` completes with exit code 0
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/nabildorhmi/Desktop/project/Rouh/Rouh UI" && npm run build && grep -c 'dev/design-preview' src/App.tsx</automated>
  </verify>
  <done>src/pages/DevDesignPreview.tsx renders both shared sections against both agency and podcast data slices; App.tsx registers the route additively; project builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify scroll-reveal, reduced-motion, and mobile layout</name>
  <files>src/pages/DevDesignPreview.tsx, src/App.tsx</files>
  <action>Run the dev server and manually confirm the scroll-reveal micro-interaction, reduced-motion gating, correct testimonial/step counts, and mobile responsiveness described below — no code changes in this task, verification only.</action>
  <what-built>A temporary dev-only route at /dev/design-preview rendering TestimonialsSection (agency slice, then podcast slice) and ProcessSection (agency steps, then podcast steps), with scroll-reveal + stagger motion on both sections' grids.</what-built>
  <how-to-verify>
    1. Run `npm run dev` from the project root and open the printed local URL (e.g. http://localhost:5173) plus `/dev/design-preview`.
    2. Scroll down the page slowly: confirm each testimonial card and each process step fades in and slides up slightly (not instantly, not jarring) as it enters the viewport, with a slight stagger between cards/steps in the same row.
    3. Confirm you see 2 testimonial cards under "What clients say" (Sarah Chen / Marcus Webb) and 2 under "What renters say" (Priya Ramesh / David Okafor) — no hover highlight/border-color-change on any testimonial card when you mouse over it.
    4. Confirm 4 numbered steps render under "Working with the agency" and 4 under "Booking the studio", each with a "Step N" orange eyebrow label.
    5. Enable your OS's "Reduce motion" accessibility setting, reload the page, and scroll again — content should now appear immediately with no fade/slide animation at all (not just faster/smaller).
    6. Resize the browser to a narrow (mobile) width — grids should collapse to a single column without overlap or overflow.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (e.g. wrong card count, animation not respecting reduced-motion, layout break on mobile)</resume-signal>
  <verify>
    <human-check>Scroll-reveal animates on both sections, reduced-motion disables it entirely, correct card/step counts render, layout is usable at mobile width</human-check>
  </verify>
  <done>Human has typed "approved" confirming scroll-reveal, reduced-motion gating, data counts, and mobile layout all behave correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Public dev-preview route (/dev/design-preview) → deployed static bundle | Route is registered in the client-side router like any other page; reachable by URL if this build is ever deployed before removal |
| Client-side routing (React Router) → browser history/URL | Standard SPA route registration, no server-side handler, no authorization boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-01-06 | Information Disclosure | src/pages/DevDesignPreview.tsx reachable via public URL if deployed before removal | accept | Route renders only fictional placeholder sample data (Sarah Chen/Lumen Skincare etc., per D-08) — no real credentials, PII, or business-sensitive content; file is clearly marked `TEMPORARY` in-code so it is discoverable and removable in Phase 2/3 cleanup; frontend-only static site has no sensitive data anywhere regardless |
| T-01-07 | Tampering | src/App.tsx route registration | accept | Client-side React Router only, no server-side route handling; identical trust level to every other existing public route (`/`, `/agency`, `/podcast`, `/contact`) — no new authorization boundary introduced |
</threat_model>

<verification>
Automated: `npm run build` exits 0, `grep` checks confirm route wiring and TEMPORARY marker. Manual: the checkpoint:human-verify task above confirms the scroll-reveal interaction, reduced-motion gating, correct data counts, and mobile responsiveness — these cannot be fully proven by grep/build alone since they are visual/behavioral.
</verification>

<success_criteria>
Roadmap Phase 1 Success Criterion 4 is met: a real micro-interaction (scroll-reveal on TestimonialsSection/ProcessSection) is visibly working in a running instance of the app, confirmed by human verification, using the actual shared components and actual sample data — not a disconnected demo.
</success_criteria>

<output>
Create `.planning/phases/01-design-system-contract-shared-primitives/01-03-SUMMARY.md` when done
</output>
