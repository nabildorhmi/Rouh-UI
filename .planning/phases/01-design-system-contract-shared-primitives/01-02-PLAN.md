---
phase: 01-design-system-contract-shared-primitives
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/shared/TestimonialCard.tsx
  - src/components/shared/TestimonialsSection.tsx
  - src/components/shared/ProcessStepItem.tsx
  - src/components/shared/ProcessSection.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "TestimonialsSection renders a balanced-looking set of quote cards correctly when given either agency-tagged or podcast-tagged sample data, with no page-specific code required (Roadmap Success Criterion 2)"
    - "ProcessSection renders a how-it-works step sequence correctly when given either agency or podcast sample data, with no page-specific code required (Roadmap Success Criterion 3)"
    - "TestimonialCard has zero hover affordance since it is non-interactive (per 01-UI-SPEC.md Motion table)"
    - "Both sections return null on an empty array instead of rendering empty-state copy (Copywriting Contract)"
    - "Neither component contains a mode/variant prop or internal branch keyed on page name — page-awareness stays out (Pitfall 5)"
  artifacts:
    - path: "src/components/shared/TestimonialCard.tsx"
      provides: "Presentational, non-interactive testimonial card"
      contains: "rounded-2xl border border-black/10 p-6"
    - path: "src/components/shared/TestimonialsSection.tsx"
      provides: "Section wrapper composing SectionHeading + a responsive scroll-reveal grid of TestimonialCard"
      contains: "from \"motion/react\""
    - path: "src/components/shared/ProcessStepItem.tsx"
      provides: "Presentational numbered process step"
      min_lines: 10
    - path: "src/components/shared/ProcessSection.tsx"
      provides: "Section wrapper composing SectionHeading + a scroll-reveal sequence of ProcessStepItem"
      contains: "from \"motion/react\""
  key_links:
    - from: "src/components/shared/TestimonialsSection.tsx"
      to: "src/components/ui/SectionHeading.tsx"
      via: "import + compose for eyebrow/title/description"
      pattern: "SectionHeading"
    - from: "src/components/shared/TestimonialsSection.tsx"
      to: "src/components/shared/TestimonialCard.tsx"
      via: "grid .map() render"
      pattern: "TestimonialCard"
    - from: "src/components/shared/ProcessSection.tsx"
      to: "src/components/shared/ProcessStepItem.tsx"
      via: "steps .map() render"
      pattern: "ProcessStepItem"
---

<objective>
Build the two page-agnostic shared components this phase exists to deliver — `TestimonialsSection`/`TestimonialCard` and `ProcessSection`/`ProcessStepItem` — as pure presentational components that consume the types and sample data from Plan 01-01, styled exactly per `01-UI-SPEC.md`'s locked token contract, with the scroll-reveal micro-interaction wired via `motion/react`.

Purpose: these are the reusable trust primitives Phases 2 (Agency) and 3 (Podcast) will consume identically to enforce "equal weight" structurally rather than per-page judgment. Getting them pixel/token-accurate and truly page-agnostic here prevents drift downstream.
Output: 4 new component files in `src/components/shared/`, each following the exact styling tokens and motion pattern specified in `01-UI-SPEC.md` and `01-RESEARCH.md`.
</objective>

<execution_context>
@/Users/nabildorhmi/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/workflows/execute-plan.md
@/Users/nabildorhmi/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-system-contract-shared-primitives/01-UI-SPEC.md
@.planning/phases/01-design-system-contract-shared-primitives/01-RESEARCH.md
@.planning/phases/01-design-system-contract-shared-primitives/01-PATTERNS.md
@.planning/phases/01-design-system-contract-shared-primitives/01-01-SUMMARY.md

<interfaces>
<!-- Types this plan's components import — created by Plan 01-01, treat as locked contracts -->
From src/types/index.ts (added by Plan 01-01):
```typescript
export interface Testimonial {
  id: string;
  category: "agency" | "podcast" | "both";
  quote: string;
  name: string;
  role: string;
}

export interface ProcessStep {
  step: number;
  title: string;
  description: string;
}
```

<!-- Existing components this plan composes, unchanged -->
From src/components/ui/SectionHeading.tsx (existing, reuse as-is):
```typescript
interface SectionHeadingProps {
  eyebrow?: string;
  title: string;
  description?: string;
  align?: "left" | "center";
}
export function SectionHeading({ eyebrow, title, description, align = "left" }: SectionHeadingProps)
```

From src/components/ui/GraphicAccent.tsx (existing, reuse sparingly — at most 1 per section per D-10):
```typescript
interface GraphicAccentProps {
  variant: 1 | 2 | 3 | 4;
  className?: string;
}
export function GraphicAccent({ variant, className = "" }: GraphicAccentProps)
```

<!-- Section shell analog to copy verbatim (container + heading composition) -->
From src/components/home/AboutSection.tsx:
```tsx
<section className="relative mx-auto max-w-6xl px-4 sm:px-6 py-20 sm:py-28 overflow-hidden">
  <GraphicAccent variant={2} className="absolute left-0 top-0 h-32 w-32 -translate-x-1/3 -translate-y-1/4 opacity-10" />
  <SectionHeading eyebrow="..." title="..." align="center" description="..." />
</section>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TestimonialCard + TestimonialsSection (scroll-reveal, the phase's proof-of-wiring pattern)</name>
  <files>src/components/shared/TestimonialCard.tsx, src/components/shared/TestimonialsSection.tsx</files>
  <read_first>src/components/ui/SectionHeading.tsx, src/components/ui/GraphicAccent.tsx, src/components/home/AboutSection.tsx, src/components/agency/ServicesGrid.tsx, .planning/phases/01-design-system-contract-shared-primitives/01-UI-SPEC.md, .planning/phases/01-design-system-contract-shared-primitives/01-RESEARCH.md</read_first>
  <action>
Create src/components/shared/TestimonialCard.tsx: named export `TestimonialCard`, props typed via `TestimonialCardProps { testimonial: Testimonial }` (import type from "../../types"), shell exactly `rounded-2xl border border-black/10 p-6` (no `hover:` classes — non-interactive per 01-UI-SPEC.md Motion table row "Hover — non-interactive cards"), quote rendered `text-base text-black/80` wrapped in curly/smart quotes, name `text-sm font-bold text-black`, role `text-sm text-black/60`. Zero conditional classes needed (no `clsx`/`cn()` required for this component).

Create src/components/shared/TestimonialsSection.tsx: named export `TestimonialsSection`, props `TestimonialsSectionProps { testimonials: Testimonial[]; eyebrow?: string; title: string; description?: string }`. Return `null` immediately if `testimonials.length === 0` (Copywriting Contract — never an empty-state message). Otherwise render `<section className="mx-auto max-w-6xl px-4 sm:px-6 py-20 sm:py-28">` containing `<SectionHeading eyebrow={eyebrow} title={title} description={description} align="center" />` followed by a `motion.div` grid with className `mt-12 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3`. Import `{ motion, useReducedMotion } from "motion/react"` (never bare `motion` or `framer-motion` — Pitfall 2). Call `const shouldReduceMotion = useReducedMotion();` and gate: `initial={shouldReduceMotion ? false : "hidden"}`, `whileInView={shouldReduceMotion ? undefined : "visible"}`, `viewport={{ once: true, margin: "-80px" }}`, `variants={{ hidden: {}, visible: { transition: { staggerChildren: 0.08 } } }}`. Each child is a `motion.div` keyed by `testimonial.id` with `variants={{ hidden: { opacity: 0, y: 16 }, visible: { opacity: 1, y: 0, transition: { duration: 0.4, ease: "easeOut" } } }}` wrapping a `<TestimonialCard testimonial={t} />`. No `GraphicAccent` required by default (optional, at most 1 per section if used — do not add one unless it reads better, and if added use exactly the `AboutSection.tsx` placement/opacity pattern).
  </action>
  <acceptance_criteria>
    - `grep -n 'from "motion/react"' src/components/shared/TestimonialsSection.tsx` returns a match (not bare "motion" or "framer-motion")
    - `grep -c 'hover:' src/components/shared/TestimonialCard.tsx` equals 0
    - `grep -c 'rounded-2xl border border-black/10 p-6' src/components/shared/TestimonialCard.tsx` is at least 1
    - `grep -c 'testimonials.length === 0' src/components/shared/TestimonialsSection.tsx` is at least 1 and is followed by a `return null`
    - `grep -c 'useReducedMotion' src/components/shared/TestimonialsSection.tsx` is at least 1
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/nabildorhmi/Desktop/project/Rouh/Rouh UI" && npx tsc -b --noEmit && grep -c 'from "motion/react"' src/components/shared/TestimonialsSection.tsx && grep -c 'hover:' src/components/shared/TestimonialCard.tsx</automated>
  </verify>
  <done>TestimonialCard renders a non-interactive bordered card exactly matching UI-SPEC tokens; TestimonialsSection composes SectionHeading + a responsive grid with correctly-gated scroll-reveal motion; both compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build ProcessStepItem + ProcessSection (same motion pattern, connected-step layout)</name>
  <files>src/components/shared/ProcessStepItem.tsx, src/components/shared/ProcessSection.tsx</files>
  <read_first>src/components/agency/PortfolioShowcase.tsx, src/components/ui/SectionHeading.tsx, src/components/home/AboutSection.tsx, .planning/phases/01-design-system-contract-shared-primitives/01-UI-SPEC.md, .planning/phases/01-design-system-contract-shared-primitives/01-RESEARCH.md</read_first>
  <action>
Create src/components/shared/ProcessStepItem.tsx: named export `ProcessStepItem`, props `ProcessStepItemProps { step: number; title: string; description: string }`, shell `flex flex-col gap-2`, eyebrow label `<span className="text-xs font-bold uppercase tracking-[0.2em] text-orange">Step {step}</span>` (same eyebrow token as `SectionHeading.tsx` line 14 — reused verbatim), title `<h3 className="text-2xl font-black text-black">{title}</h3>`, description `<p className="text-base text-black/60">{description}</p>`. No connector-line visual (Open Question 2 resolved: numbered eyebrow label satisfies "connected-step" without introducing a new border/line token not present in 01-UI-SPEC.md).

Create src/components/shared/ProcessSection.tsx: named export `ProcessSection`, props `ProcessSectionProps { steps: ProcessStep[]; eyebrow?: string; title: string; description?: string }` (import type `ProcessStep` from "../../types"). Return `null` immediately if `steps.length === 0`. Otherwise render the same `<section className="mx-auto max-w-6xl px-4 sm:px-6 py-20 sm:py-28">` + `SectionHeading` shell as `TestimonialsSection`, followed by a `motion.div` laying out `steps.map()` in a responsive grid/flex row (`grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-4`, matching the 4-step data shape from Plan 01-01) using the identical `motion/react` scroll-reveal + `useReducedMotion` gating pattern as `TestimonialsSection` (same `initial`/`whileInView`/`viewport`/`variants`/`staggerChildren: 0.08` values — do not invent a second motion timing). Each child `motion.div` is keyed by `step.step` and wraps a `<ProcessStepItem step={step.step} title={step.title} description={step.description} />`.
  </action>
  <acceptance_criteria>
    - `grep -c 'from "motion/react"' src/components/shared/ProcessSection.tsx` is at least 1
    - `grep -c 'steps.length === 0' src/components/shared/ProcessSection.tsx` is at least 1
    - `grep -c 'staggerChildren: 0.08' src/components/shared/ProcessSection.tsx` is at least 1 (same timing as TestimonialsSection — no invented second motion spec)
    - `grep -c 'text-xs font-bold uppercase tracking-\[0.2em\] text-orange' src/components/shared/ProcessStepItem.tsx` is at least 1
    - `grep -c "mode\|variant:" src/components/shared/ProcessSection.tsx` equals 0 (no page-awareness leak, Pitfall 5)
    - `npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/nabildorhmi/Desktop/project/Rouh/Rouh UI" && npx tsc -b --noEmit && grep -c 'from "motion/react"' src/components/shared/ProcessSection.tsx</automated>
  </verify>
  <done>ProcessStepItem renders a numbered, presentational step; ProcessSection composes SectionHeading + a scroll-revealed sequence of steps using the identical motion pattern as TestimonialsSection; both compile clean; neither component has page-specific branching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Testimonial/ProcessStep prop data → JSX render (TestimonialCard, ProcessStepItem) | Data flows in as typed props from Plan 01-01's static fixtures; render path must stay text-interpolation only |
| `motion/react` scroll-reveal → browser paint/layout thread | Client-side animation library executing on every section mount; must respect reduced-motion preference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-01-03 | Tampering / Information Disclosure | TestimonialCard.tsx (`{testimonial.quote}`), ProcessStepItem.tsx (`{step.description}`) | mitigate | Render exclusively via JSX text interpolation (React auto-escapes); never use `dangerouslySetInnerHTML` for testimonial/step copy — no requirement in 01-UI-SPEC.md calls for rich-text content |
| T-01-04 | Denial of Service (minor, availability/UX) | `motion/react` scroll-reveal on TestimonialsSection/ProcessSection | accept | `useReducedMotion()` gates animation entirely per D-06; client-side only, no perf-critical path, `viewport={{ once: true }}` prevents repeated re-trigger overhead |
| T-01-05 | Elevation of Privilege (N/A check) | N/A — no interactive/clickable surface on TestimonialCard or ProcessStepItem | accept | Both components are presentational with zero event handlers, zero user input; no privilege boundary exists in this plan's scope |
</threat_model>

<verification>
Run `npx tsc -b --noEmit` (must exit 0). Grep-verify motion import path, empty-array guards, absence of hover classes on TestimonialCard, and absence of mode/variant page-awareness leaks per each task's acceptance_criteria.
</verification>

<success_criteria>
TestimonialsSection and ProcessSection both compile, both return null on empty data, both use the identical motion/react scroll-reveal + useReducedMotion pattern, TestimonialCard has zero hover affordance, and neither section contains any page-specific (agency/podcast) branching logic.
</success_criteria>

<output>
Create `.planning/phases/01-design-system-contract-shared-primitives/01-02-SUMMARY.md` when done
</output>
