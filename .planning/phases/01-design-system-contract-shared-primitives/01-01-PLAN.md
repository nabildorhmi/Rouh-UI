---
phase: 01-design-system-contract-shared-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/styles/index.css
  - src/types/index.ts
  - src/data/testimonials.ts
  - src/data/process.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "New animation/utility/icon dependencies (motion, tw-animate-css, clsx, tailwind-merge, class-variance-authority, lucide-react) are installed and resolve without peer-dependency conflicts against React 19.2.7"
    - "Testimonial and ProcessStep types exist in the single src/types/index.ts file and are usable by downstream shared components"
    - "Sample testimonial data maintains a balanced 2+-per-category split (agency/podcast) per D-09, using realistic fictional attribution per D-07/D-08"
    - "Sample process-step data exists for both the agency and podcast lines, 4 steps each, matching the checker-approved copy in 01-UI-SPEC.md / 01-RESEARCH.md"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Testimonial and ProcessStep interfaces appended to the existing single types module"
      contains: "export interface Testimonial"
    - path: "src/data/testimonials.ts"
      provides: "Balanced sample testimonial fixture (2 agency + 2 podcast)"
      min_lines: 20
    - path: "src/data/process.ts"
      provides: "Grouped agency/podcast process-step fixture"
      contains: "agency:"
    - path: "src/styles/index.css"
      provides: "tw-animate-css wired in as a build-time CSS import"
      contains: "@import \"tw-animate-css\";"
  key_links:
    - from: "src/data/testimonials.ts"
      to: "src/types/index.ts"
      via: "import type { Testimonial }"
      pattern: "import type \\{ Testimonial \\} from"
    - from: "src/data/process.ts"
      to: "src/types/index.ts"
      via: "import type { ProcessStep }"
      pattern: "import type \\{ ProcessStep \\} from"
---

<objective>
Install this phase's net-new dependencies (motion, tw-animate-css, clsx, tailwind-merge, class-variance-authority, lucide-react) and lay down the interface contracts — `Testimonial`/`ProcessStep` types plus their sample data fixtures — that Plan 01-02's shared components will consume.

Purpose: this is the interface-first foundation wave of Phase 1's walking skeleton. Nothing downstream (`TestimonialCard`, `TestimonialsSection`, `ProcessStepItem`, `ProcessSection`, the Wave 3 verification route) can be built correctly without these types, this sample data, and these libraries already installed and resolvable.
Output: `package.json`/`package-lock.json` updated with 6 new dependencies; `src/styles/index.css` wired to `tw-animate-css`; `src/types/index.ts` extended with `Testimonial`/`ProcessStep`; `src/data/testimonials.ts` and `src/data/process.ts` created as static, balanced sample fixtures.
</objective>

<execution_context>
@/Users/nabildorhmi/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/workflows/execute-plan.md
@/Users/nabildorhmi/.claude/plugins/cache/gsd-plugin/gsd/4.4.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-design-system-contract-shared-primitives/01-UI-SPEC.md
@.planning/phases/01-design-system-contract-shared-primitives/01-RESEARCH.md
@.planning/phases/01-design-system-contract-shared-primitives/01-CONTEXT.md

<interfaces>
<!-- Existing src/types/index.ts conventions the new interfaces must match exactly -->
From src/types/index.ts (existing, unmodified interfaces to append after):
```typescript
export interface PortfolioItem {
  title: string;
  category: string;
  description: string;
}

export interface GuestTier {
  id: string;
  name: string;
  price: string;
  tagline: string;
  features: string[];
  highlighted?: boolean;
}
```

<!-- Existing src/data/ file shape to replicate for testimonials.ts (flat array) -->
From src/data/portfolio.ts (verbatim shape to replicate):
```typescript
import type { PortfolioItem } from "../types";

// Placeholder case studies — swap in real client work when available.
export const portfolio: PortfolioItem[] = [
  { title: "Placeholder Client A", category: "Brand Launch", description: "..." },
];
```

<!-- src/styles/index.css current line 1 — exact insertion point -->
From src/styles/index.css (line 1, current):
```css
@import "tailwindcss";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install net-new dependencies and wire tw-animate-css</name>
  <files>package.json, package-lock.json, src/styles/index.css</files>
  <read_first>package.json, src/styles/index.css</read_first>
  <action>
Run `npm install motion@12.43.0 clsx@2.1.1 tailwind-merge@3.6.0 class-variance-authority@0.7.1 lucide-react@1.27.0` to add these 5 packages to `dependencies`. Run `npm install -D tw-animate-css@1.4.0` separately (dev dependency only — it is never imported in JS, only referenced via a single CSS `@import`, per 01-RESEARCH.md Pitfall 4). Do not use `npm install <all six>` in one command — `tw-animate-css` must land in `devDependencies`, not `dependencies`.

Edit src/styles/index.css: insert `@import "tw-animate-css";` as a new line immediately after the existing `@import "tailwindcss";` (currently line 1). Do not touch any other line in this file — no `@theme`, `@font-face`, or `@layer` edits this task.
  </action>
  <acceptance_criteria>
    - package.json "dependencies" object contains keys: motion, clsx, tailwind-merge, class-variance-authority, lucide-react
    - package.json "devDependencies" object contains key: tw-animate-css
    - src/styles/index.css line 2 is exactly `@import "tw-animate-css";` and line 1 remains `@import "tailwindcss";` unchanged
    - `npm run build` completes with exit code 0
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/nabildorhmi/Desktop/project/Rouh/Rouh UI" && npm run build</automated>
  </verify>
  <done>All 6 packages present in package.json under the correct dependency type, tw-animate-css imported in index.css, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Define Testimonial/ProcessStep types and create sample data fixtures</name>
  <files>src/types/index.ts, src/data/testimonials.ts, src/data/process.ts</files>
  <read_first>src/types/index.ts, src/data/portfolio.ts, src/data/guestTiers.ts, .planning/phases/01-design-system-contract-shared-primitives/01-UI-SPEC.md, .planning/phases/01-design-system-contract-shared-primitives/01-RESEARCH.md</read_first>
  <action>
Append two new interfaces to src/types/index.ts, after the existing `GuestTier` interface block: `Testimonial` with fields `id: string`, `category: "agency" | "podcast" | "both"`, `quote: string`, `name: string`, `role: string`; and `ProcessStep` with fields `step: number`, `title: string`, `description: string`. PascalCase names, no `Props` suffix, matching the file's existing interface style exactly.

Create src/data/testimonials.ts: `import type { Testimonial } from "../types";`, a top-of-file comment noting this is realistic placeholder content pending real client/renter quotes (per D-07/D-08 — phrase it as finished-reading placeholder content, not a "TBD" marker), then `export const testimonials: Testimonial[] = [...]` with exactly these 4 records (ids t1-t4, verbatim copy from 01-UI-SPEC.md's Sample testimonial copy table — this is the checker-approved content deliverable, not just an example to riff on): t1 category "agency", quote "Rouh didn't just make us look better — they rebuilt how we talk about the product. Our funnel numbers moved within the first month.", name "Sarah Chen", role "Founder, Lumen Skincare"; t2 category "agency", quote "We've worked with three agencies before Rouh. This is the first one that actually shipped on the timeline they gave us.", name "Marcus Webb", role "Marketing Director, Northfield Outdoor"; t3 category "podcast", quote "Booked the studio same week, showed up, and the crew had everything dialed in before we sat down. Zero setup stress.", name "Priya Ramesh", role "Host, The Build Sheet"; t4 category "podcast", quote "Rented the full kit for a two-day shoot — cameras, lighting, the works. Recordings came out broadcast-ready straight off the gear.", name "David Okafor", role "Co-host, Second Opinion Podcast".

Create src/data/process.ts: `import type { ProcessStep } from "../types";`, a top-of-file comment noting this is the sample step data proven by this phase's shared `ProcessSection` component, then `export const processSteps: { agency: ProcessStep[]; podcast: ProcessStep[] } = {...}` (grouped object, NOT a flat array — deliberate deviation from the rest of src/data/, per 01-RESEARCH.md's Architecture Patterns) using exactly: agency = [{step:1,title:"Discovery call",description:"We learn your brand, goals, and where things are stuck."},{step:2,title:"Scope & plan",description:"You get a clear plan — deliverables, timeline, no surprises."},{step:3,title:"We build",description:"Strategy, content, and campaigns move from plan to shipped work."},{step:4,title:"Launch & iterate",description:"We track what's working and refine as results come in."}]; podcast = [{step:1,title:"Pick a package",description:"Choose your session length and gear tier."},{step:2,title:"Book your session",description:"Reserve a date — dry-hire or staffed, your call."},{step:3,title:"Show up & record",description:"The room and gear are dialed in before you arrive."},{step:4,title:"Walk away with your files",description:"Leave with your raw recording, ready for post."}].
  </action>
  <acceptance_criteria>
    - `grep -c "export interface Testimonial" src/types/index.ts` equals 1
    - `grep -c "export interface ProcessStep" src/types/index.ts` equals 1
    - src/data/testimonials.ts exports exactly 4 records: exactly 2 with `category: "agency"` and exactly 2 with `category: "podcast"`
    - src/data/process.ts exports an object with both an `agency` key and a `podcast` key, each an array of exactly 4 entries
    - `npx tsc -b --noEmit` exits 0 with no errors referencing testimonials.ts, process.ts, or types/index.ts
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/nabildorhmi/Desktop/project/Rouh/Rouh UI" && npx tsc -b --noEmit && grep -c '"agency"' src/data/testimonials.ts && grep -c '"podcast"' src/data/testimonials.ts</automated>
  </verify>
  <done>Testimonial and ProcessStep types exist in src/types/index.ts; testimonials.ts and process.ts export the exact balanced sample data; TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry → project dependency tree | `npm install` pulls third-party code (motion, tw-animate-css, clsx, tailwind-merge, class-variance-authority, lucide-react) into the build; supply-chain risk surface |
| Developer-authored static data → future JSX render (consumed by Plan 01-02) | Data is trusted (not user input), but this is the payload later components must render safely via JSX text interpolation, not raw HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-01-01 | Tampering | npm install of motion/clsx/tailwind-merge/class-variance-authority/lucide-react/tw-animate-css | mitigate | Already pinned to exact registry-verified versions in this task's action; 01-RESEARCH.md's Package Legitimacy Audit confirms `slopcheck [OK]` for all six, zero `postinstall` scripts, clean React 19.2.7 peer-dep ranges — install exactly as specified, no version drift |
| T-01-02 | Information Disclosure | src/data/testimonials.ts fictional attribution content | accept | Placeholder data is fictional/non-PII per D-08 (no real client/renter data is handled in this phase); low risk |
| T-01-SC | Tampering | npm package installs (supply-chain) | mitigate | Package Legitimacy Audit already completed in 01-RESEARCH.md for all 6 packages ([OK] verdict, no postinstall scripts, verified registry provenance) — no `[ASSUMED]`/`[SUS]` packages present, so no blocking human-verify checkpoint is required for this task |
</threat_model>

<verification>
Run `npm run build` (must exit 0) and `npx tsc -b --noEmit` (must exit 0). Confirm package.json's dependency/devDependency split matches Task 1's acceptance criteria. Confirm testimonials.ts/process.ts data counts match Task 2's acceptance criteria.
</verification>

<success_criteria>
All 6 dependencies installed in the correct dependency type; tw-animate-css wired into src/styles/index.css; Testimonial and ProcessStep types exist in src/types/index.ts; testimonials.ts and process.ts contain the exact, balanced sample data specified in 01-UI-SPEC.md; the project still builds cleanly.
</success_criteria>

<output>
Create `.planning/phases/01-design-system-contract-shared-primitives/01-01-SUMMARY.md` when done
</output>
