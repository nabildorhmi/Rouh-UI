# Base URL for API requests.
# In local development, /api points to Vite's dev-server proxy (forwarding to http://localhost:8000).
# In production, set to absolute backend URL (e.g. https://rouh.com/api or https://api.rouh.com).
VITE_API_BASE_URL=/api
